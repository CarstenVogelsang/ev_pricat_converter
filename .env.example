# Flask Configuration
FLASK_APP=run.py
FLASK_CONFIG=development
SECRET_KEY=your-secret-key-here

# Database (optional, defaults to SQLite in instance folder)
# DATABASE_URL=sqlite:///instance/pricat.db

# Initial Admin User (required for seed-essential)
INITIAL_ADMIN_EMAIL=admin@your-domain.com
INITIAL_ADMIN_PASSWORD=change-this-password

# Playwright/Claude Code Test Credentials (not used by Flask app)
EV247_ADMIN_EMAIL=carsten.vogelsang@e-vendo.de
EV247_ADMIN_PASSWORD=admin123
