# Nixpacks configuration for Coolify deployment
# https://nixpacks.com/docs/configuration/file

[phases.setup]
nixPkgs = ["python311"]

[start]
cmd = "uv run flask init-db && uv run flask seed && uv run gunicorn -w 4 -b 0.0.0.0:3000 'app:create_app()'"
