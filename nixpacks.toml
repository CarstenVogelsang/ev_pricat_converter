# Nixpacks configuration for Coolify deployment
# https://nixpacks.com/docs/configuration/file

providers = ["python"]

[phases.setup]
aptGet = false

[start]
cmd = "flask init-db && flask seed && flask seed-users && gunicorn -w 4 -b 0.0.0.0:3000 'app:create_app()'"
